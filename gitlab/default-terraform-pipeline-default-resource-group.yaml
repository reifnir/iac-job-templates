# This is the same template as default-terraform-pipeline.yaml, except that it already has a value for the resource_group field.
# This makes sense for any projects that only involve a single set of terraform state/environment.

include:
  - local: gitlab/default-terraform-pipeline.yaml

plan:
  resource_group: terraform

apply:
  resource_group: terraform
