# job-templates

Will write tests later. Can't get so mired in yak shedding that I don't get anything done!

Note: How to quickly run a checkov locally:

```bash
	docker run -it --rm -v "$1(pwd):/src" -w /src --entrypoint="" bridgecrew/checkov:latest sh -c "checkov --directory /src"
```
